name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  # Probe the Boundless-Studios self-hosted desktop fleet and choose where lint
  # runs — the fleet (gaia-ci-desktop) when it's online and authorized for this
  # repo, else ubuntu-latest. Never hangs on an unavailable fleet.
  setup:
    runs-on: ubuntu-latest
    outputs:
      runs-on: ${{ steps.choose.outputs.runs-on }}
    steps:
      - id: choose
        env:
          PROBE_PAT: ${{ secrets.RUNNERS_PROBE_PAT }}
          GH_TOKEN: ${{ secrets.RUNNERS_PROBE_PAT }}
        run: |
          set -e
          if [ "${{ github.event_name }}" = "pull_request" ] && \
             [ "${{ github.event.pull_request.head.repo.full_name }}" != "${{ github.repository }}" ]; then
            echo "::notice::Fork PR — using ubuntu-latest."
            echo 'runs-on=["ubuntu-latest"]' >> "$GITHUB_OUTPUT"; exit 0
          fi
          if [ -z "$PROBE_PAT" ]; then
            echo "::notice::RUNNERS_PROBE_PAT not set; using ubuntu-latest. Add the secret to route CI to the desktop fleet."
            echo 'runs-on=["ubuntu-latest"]' >> "$GITHUB_OUTPUT"; exit 0
          fi
          ONLINE=$(gh api "repos/${{ github.repository }}/actions/runners" \
            --jq '[.runners[] | select(.status=="online") | select([.labels[].name] | index("gaia-ci-desktop"))] | length' 2>/dev/null || echo 0)
          if [ "${ONLINE:-0}" -gt 0 ]; then
            echo "Desktop fleet: ${ONLINE} online runner(s) — routing CI there."
            echo 'runs-on=["gaia-ci-desktop"]' >> "$GITHUB_OUTPUT"
          else
            echo "::warning::No online gaia-ci-desktop runners authorized for this repo; using ubuntu-latest."
            echo 'runs-on=["ubuntu-latest"]' >> "$GITHUB_OUTPUT"
          fi

  lint:
    needs: setup
    runs-on: ${{ fromJSON(needs.setup.outputs.runs-on) }}
    steps:
      - uses: actions/checkout@v4

      - name: Syntax (bash -n)
        run: |
          set -e
          bash -n bin/worktree-deck.sh
          for f in lib/*.sh; do
            echo "bash -n $f"
            bash -n "$f"
          done
          for f in test/*.sh; do
            echo "bash -n $f"
            bash -n "$f"
          done

      - name: Tests
        run: |
          set -e
          bash test/refresh-pr-data-timeout.sh
          bash test/render-timestamps.sh

      - name: ShellCheck
        uses: ludeeus/action-shellcheck@master
        with:
          severity: error
          additional_files: 'worktree-deck.sh'
        env:
          SHELLCHECK_OPTS: -e SC1091
